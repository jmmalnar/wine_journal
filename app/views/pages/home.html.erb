<% logged_in = true %>

<h1 id="title">Amy's wine journal</h1>

<div class="pushmenu-push">

  <% if logged_in %>
    <nav class="pushmenu pushmenu-left">
      <div class="menu_links">
        <%= link_to 'my list', wines_path, class: 'menu_links' %>
        <%= link_to 'add a wine', new_wine_path, class: 'menu_links' %>
      </div>
    </nav>
  <% else %>
    <nav class="pushmenu pushmenu-left">
      <div class="menu_links">
        <p>You need to login first</p>
      </div>
    </nav>
  <% end %>

      <div class="content">

  </div>

</div>
